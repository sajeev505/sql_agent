# 📚 Learn: AI Powered SQL Query Generator

A comprehensive guide to understanding this project from scratch.

---

## 🎯 What Does This Project Do?

This application lets you **ask questions in plain English** and automatically converts them into **SQL queries** that run against your MySQL database.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   "Show me all customers from New York who ordered last month"              │
│                              │                                              │
│                              ▼                                              │
│                        ┌──────────┐                                         │
│                        │   AI     │  ← OpenAI GPT-4                         │
│                        │  Engine  │                                         │
│                        └────┬─────┘                                         │
│                             │                                               │
│                             ▼                                               │
│   SELECT * FROM customers                                                   │
│   WHERE city = 'New York'                                                   │
│   AND order_date >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH);                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Architecture Overview

The project follows a **3-tier architecture**:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ARCHITECTURE DIAGRAM                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌──────────────────────────────────────────────────────────────────┐      │
│   │                    FRONTEND (Streamlit)                          │      │
│   │                         ui.py                                    │      │
│   │  ┌─────────────┐  ┌───────────────────┐  ┌──────────────────┐   │      │
│   │  │  Sidebar    │  │   Query Input     │  │  Results Table   │   │      │
│   │  │  Explorer   │  │   Text Area       │  │  (DataFrame)     │   │      │
│   │  └─────────────┘  └───────────────────┘  └──────────────────┘   │      │
│   └──────────────────────────────┬───────────────────────────────────┘      │
│                                  │ HTTP Requests                            │
│                                  ▼                                          │
│   ┌──────────────────────────────────────────────────────────────────┐      │
│   │                    BACKEND (FastAPI)                             │      │
│   │                         app.py                                   │      │
│   │  ┌─────────────┐  ┌───────────────────┐  ┌──────────────────┐   │      │
│   │  │  /databases │  │  /generate_sql    │  │  /execute_sql    │   │      │
│   │  │  /tables    │  │  (POST)           │  │  (POST)          │   │      │
│   │  │  /columns   │  │                   │  │                  │   │      │
│   │  └──────┬──────┘  └─────────┬─────────┘  └────────┬─────────┘   │      │
│   └─────────┼───────────────────┼──────────────────────┼────────────┘      │
│             │                   │                      │                    │
│             ▼                   ▼                      ▼                    │
│   ┌──────────────────┐  ┌───────────────────┐  ┌──────────────────┐        │
│   │    database.py   │  │ query_generator.py │  │   database.py    │        │
│   │  (Schema Access) │  │  (OpenAI API)      │  │  (Query Exec)    │        │
│   └────────┬─────────┘  └─────────┬─────────┘  └────────┬─────────┘        │
│            │                      │                      │                  │
│            ▼                      ▼                      ▼                  │
│   ┌──────────────────────────────────────────────────────────────────┐      │
│   │                       DATA LAYER                                 │      │
│   │  ┌────────────────────┐        ┌────────────────────────────┐   │      │
│   │  │      MySQL         │        │      OpenAI API            │   │      │
│   │  │     Database       │        │     (GPT-4/3.5)           │   │      │
│   │  └────────────────────┘        └────────────────────────────┘   │      │
│   └──────────────────────────────────────────────────────────────────┘      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 File-by-File Breakdown

### The Request Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW DIAGRAM                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  User types: "Show all products under $50"                                   │
│                          │                                                   │
│                          ▼                                                   │
│  ┌────────────────────────────────────────┐                                  │
│  │             ui.py (Streamlit)          │                                  │
│  │  - Captures user input                 │                                  │
│  │  - Sends POST to /generate_sql         │                                  │
│  └───────────────────┬────────────────────┘                                  │
│                      │                                                       │
│                      ▼                                                       │
│  ┌────────────────────────────────────────┐                                  │
│  │             app.py (FastAPI)           │                                  │
│  │  - Receives request                    │                                  │
│  │  - Calls query_generator               │                                  │
│  └───────────────────┬────────────────────┘                                  │
│                      │                                                       │
│                      ▼                                                       │
│  ┌────────────────────────────────────────┐                                  │
│  │       query_generator.py               │                                  │
│  │  1. fetch_limited_schema()             │◄───── database.py (get schema)   │
│  │  2. Build prompt with schema           │                                  │
│  │  3. Call OpenAI API                    │◄───── OpenAI GPT-4               │
│  │  4. clean_sql_query()                  │                                  │
│  │  5. Return SQL                         │                                  │
│  └───────────────────┬────────────────────┘                                  │
│                      │                                                       │
│                      ▼                                                       │
│  Returns: "SELECT * FROM products WHERE price < 50"                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔍 Understanding Each File

### 1️⃣ `requirements.txt` - Dependencies

Lists all Python packages needed:

| Package | Purpose |
|---------|---------|
| `openai` | Connect to OpenAI's GPT models |
| `sqlalchemy` | Database ORM & connection pooling |
| `pymysql` | MySQL database driver |
| `fastapi` | Modern async web framework |
| `uvicorn` | ASGI server for FastAPI |
| `streamlit` | Rapid UI prototyping |
| `python-dotenv` | Load environment variables |
| `sqlparse` | SQL parsing & validation |

---

### 2️⃣ `.env` - Configuration

```
                    ┌─────────────────────────────────────┐
                    │           .env File                 │
                    ├─────────────────────────────────────┤
                    │                                     │
                    │  MYSQL_HOST ──────► localhost       │
                    │  MYSQL_USER ──────► root            │
                    │  MYSQL_PASSWORD ─► ********         │
                    │  MYSQL_DATABASE ─► test_db          │
                    │  MYSQL_PORT ─────► 3306             │
                    │                                     │
                    │  OPENAI_API_KEY ─► sk-proj-...      │
                    │                                     │
                    └─────────────────────────────────────┘
```

**Why use .env?**
- Keeps secrets out of code
- Easy to change per environment (dev/prod)
- Never committed to Git (add to .gitignore!)

---

### 3️⃣ `database.py` - Database Layer

**Purpose:** Connects to MySQL and retrieves schema information.

```python
# Key Functions:

get_db_connection()   →  Returns active MySQL connection
list_databases()      →  Returns ['db1', 'db2', 'db3', ...]
list_tables(db)       →  Returns ['users', 'orders', 'products', ...]
list_columns(db, tbl) →  Returns [{'Field': 'id', 'Type': 'int'}, ...]
```

**How SQLAlchemy Works:**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SQLAlchemy Connection                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   DATABASE_URL = "mysql+pymysql://user:pass@host:port/database"             │
│                        ▲    ▲         ▲    ▲     ▲                          │
│                        │    │         │    │     │                          │
│                   dialect  driver   host  port  database                    │
│                                                                             │
│   engine = create_engine(DATABASE_URL)                                      │
│      │                                                                      │
│      └──► Manages connection pooling, transactions, etc.                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 4️⃣ `query_generator.py` - AI Brain

**The most important file!** This is where natural language becomes SQL.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    QUERY GENERATION PIPELINE                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Step 1: Fetch Schema                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  fetch_limited_schema("test_db")                                    │    │
│  │                                                                     │    │
│  │  Returns: {                                                         │    │
│  │    "users": ["id", "name", "email", "created_at"],                  │    │
│  │    "products": ["id", "name", "price", "category"],                 │    │
│  │    "orders": ["id", "user_id", "product_id", "quantity"]            │    │
│  │  }                                                                  │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                     │                                       │
│                                     ▼                                       │
│  Step 2: Build Prompt                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  "You are an expert SQL Assistant.                                  │    │
│  │   Convert the following natural language query into MySQL.          │    │
│  │                                                                     │    │
│  │   Database Schema:                                                  │    │
│  │   {schema_json}                                                     │    │
│  │                                                                     │    │
│  │   Query: 'Show all products under $50'                              │    │
│  │                                                                     │    │
│  │   Return ONLY the SQL query."                                       │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                     │                                       │
│                                     ▼                                       │
│  Step 3: Call OpenAI API                                                    │
│  ┌───────────────┐      ┌───────────────────────────────┐                   │
│  │   Prompt      │ ───► │   OpenAI GPT-4               │                   │
│  │               │      │   temperature=0 (deterministic)│                   │
│  └───────────────┘      └────────────────┬──────────────┘                   │
│                                          │                                  │
│                                          ▼                                  │
│  Step 4: Clean Response                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  clean_sql_query(response)                                          │    │
│  │                                                                     │    │
│  │  Removes: ```sql ... ``` markdown formatting                        │    │
│  │  Returns: SELECT * FROM products WHERE price < 50                   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5️⃣ `app.py` - FastAPI Backend

**REST API that connects everything together.**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         API ENDPOINTS                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  GET /databases                                                             │
│  ├── Returns: {"databases": ["mysql", "test_db", "production"]}             │
│  └── Calls: database.list_databases()                                       │
│                                                                             │
│  GET /tables/{database_name}                                                │
│  ├── Example: GET /tables/test_db                                           │
│  ├── Returns: {"tables": ["users", "orders", "products"]}                   │
│  └── Calls: database.list_tables(database_name)                             │
│                                                                             │
│  GET /columns/{database_name}/{table_name}                                  │
│  ├── Example: GET /columns/test_db/users                                    │
│  ├── Returns: {"columns": [{"Field": "id", "Type": "int"}, ...]}            │
│  └── Calls: database.list_columns(database_name, table_name)                │
│                                                                             │
│  POST /generate_sql                                                         │
│  ├── Body: {"natural_query": "...", "database_name": "..."}                 │
│  ├── Returns: {"sql_query": "SELECT ..."}                                   │
│  └── Calls: query_generator.generate_sql_query(...)                         │
│                                                                             │
│  POST /execute_sql                                                          │
│  ├── Body: {"sql_query": "SELECT ..."}                                      │
│  ├── Returns: {"data": [...], "columns": [...]}                             │
│  └── Calls: query_generator.execute_query(...)                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Pydantic Models for Request Validation:**

```python
class QueryRequest(BaseModel):
    natural_query: str      # Required string
    database_name: str      # Required string

class ExecuteRequest(BaseModel):
    sql_query: str          # Required string
```

---

### 6️⃣ `ui.py` - Streamlit Frontend

**The user interface that makes everything accessible.**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           UI LAYOUT                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────┬─────────────────────────────────────────────────────┐ │
│  │                  │                                                     │ │
│  │  SIDEBAR         │            MAIN AREA                                │ │
│  │                  │                                                     │ │
│  │  [List Databases]│   🤖 AI Powered SQL Query Generator                 │ │
│  │                  │                                                     │ │
│  │  ▼ Select DB     │   ┌─────────────────────────────────────────────┐   │ │
│  │  ┌─────────────┐ │   │                                             │   │ │
│  │  │  test_db  ▼ │ │   │  Enter your question in plain English:     │   │ │
│  │  └─────────────┘ │   │  ________________________________________   │   │ │
│  │                  │   │  ________________________________________   │   │ │
│  │  [Show Tables]   │   │                                             │   │ │
│  │                  │   └─────────────────────────────────────────────┘   │ │
│  │  ▼ Select Table  │                                                     │ │
│  │  ┌─────────────┐ │   [Generate SQL]                                    │ │
│  │  │  users    ▼ │ │                                                     │ │
│  │  └─────────────┘ │   ┌─────────────────────────────────────────────┐   │ │
│  │                  │   │ SELECT * FROM users WHERE age > 25;         │   │ │
│  │  [Show Columns]  │   └─────────────────────────────────────────────┘   │ │
│  │                  │                                                     │ │
│  │  ┌─────────────┐ │   [Execute SQL]                                     │ │
│  │  │ Columns:    │ │                                                     │ │
│  │  │ id   | int  │ │   ┌─────────────────────────────────────────────┐   │ │
│  │  │ name | var  │ │   │ id │ name    │ age │ email                  │   │ │
│  │  │ age  | int  │ │   ├────┼─────────┼─────┼────────────────────────┤   │ │
│  │  └─────────────┘ │   │ 1  │ Alice   │ 30  │ alice@example.com      │   │ │
│  │                  │   │ 2  │ Bob     │ 28  │ bob@example.com        │   │ │
│  │                  │   └─────────────────────────────────────────────┘   │ │
│  │                  │                                                     │ │
│  └──────────────────┴─────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Key Streamlit Concepts:**

```python
# Session State - Persists data between reruns
st.session_state["databases"] = ["db1", "db2"]

# Widgets
st.sidebar.button("Click me")           # Sidebar button
st.selectbox("Choose", options)         # Dropdown
st.text_area("Enter text", height=100)  # Multi-line input
st.code("SELECT * FROM x", language="sql")  # Code block
st.dataframe(pd.DataFrame(data))        # Data table
```

---

## 🔑 Key Concepts Explained

### What is an API?

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         API (Application Programming Interface)             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   Think of an API as a WAITER in a restaurant:                              │
│                                                                             │
│   ┌────────────┐        ┌────────────┐        ┌────────────┐                │
│   │   Client   │        │    API     │        │   Server   │                │
│   │  (You)     │   ──►  │  (Waiter)  │   ──►  │  (Kitchen) │                │
│   │            │        │            │        │            │                │
│   │ "I want    │        │ Takes your │        │ Prepares   │                │
│   │  pasta"    │        │ order to   │        │ the dish   │                │
│   │            │   ◄──  │ kitchen    │   ◄──  │            │                │
│   │            │        │ Brings     │        │            │                │
│   │  🍝        │        │ back food  │        │            │                │
│   └────────────┘        └────────────┘        └────────────┘                │
│                                                                             │
│   In our app:                                                               │
│   - Client    = Streamlit UI                                                │
│   - API       = FastAPI endpoints                                           │
│   - Server    = Database + OpenAI                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### How OpenAI API Works

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         OpenAI API CALL                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   openai.chat.completions.create(                                           │
│       model="gpt-4",                    # Which AI model                    │
│       messages=[                        # Conversation history              │
│           {                                                                 │
│               "role": "system",         # AI's personality                  │
│               "content": "You are a SQL assistant"                          │
│           },                                                                │
│           {                                                                 │
│               "role": "user",           # User's message                    │
│               "content": "Convert to SQL: Show all users"                   │
│           }                                                                 │
│       ],                                                                    │
│       temperature=0                     # 0 = deterministic, 1 = creative   │
│   )                                                                         │
│                                                                             │
│   Returns:                                                                  │
│   {                                                                         │
│       "choices": [{                                                         │
│           "message": {                                                      │
│               "content": "SELECT * FROM users"    ◄─── The SQL query!       │
│           }                                                                 │
│       }]                                                                    │
│   }                                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### What is SQLAlchemy?

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SQLAlchemy - Database Toolkit                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   Without SQLAlchemy:                                                       │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  import mysql.connector                                             │   │
│   │  conn = mysql.connector.connect(host='localhost', user='root', ...) │   │
│   │  cursor = conn.cursor()                                             │   │
│   │  cursor.execute("SELECT * FROM users")                              │   │
│   │  results = cursor.fetchall()                                        │   │
│   │  cursor.close()                                                     │   │
│   │  conn.close()  # Manually manage connection!                        │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   With SQLAlchemy:                                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  from sqlalchemy import create_engine, text                         │   │
│   │  engine = create_engine("mysql+pymysql://...")                      │   │
│   │                                                                     │   │
│   │  with engine.connect() as connection:  # Auto-manages connection!  │   │
│   │      result = connection.execute(text("SELECT * FROM users"))       │   │
│   │      # Connection auto-closed here                                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   Benefits:                                                                 │
│   ✓ Connection pooling (reuses connections)                                 │
│   ✓ Automatic cleanup                                                       │
│   ✓ Works with multiple databases (MySQL, PostgreSQL, SQLite, etc.)         │
│   ✓ Protection against SQL injection (with text())                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete Request Lifecycle

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    FULL REQUEST LIFECYCLE                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. USER ACTION                                                             │
│     User types: "Find expensive products" → Clicks "Generate SQL"           │
│                                     │                                       │
│                                     ▼                                       │
│  2. FRONTEND (ui.py)                                                        │
│     POST /generate_sql                                                      │
│     Body: {                                                                 │
│       "natural_query": "Find expensive products",                           │
│       "database_name": "shop_db"                                            │
│     }                                                                       │
│                                     │                                       │
│                                     ▼                                       │
│  3. API LAYER (app.py)                                                      │
│     @app.post("/generate_sql")                                              │
│     def generate_sql_endpoint(request: QueryRequest):                       │
│         sql = generate_sql_query(request.natural_query, request.database)   │
│                                     │                                       │
│                                     ▼                                       │
│  4. LOGIC LAYER (query_generator.py)                                        │
│     a) fetch_limited_schema("shop_db")                                      │
│        └──► database.py → MySQL → {"products": ["id", "name", "price"]}     │
│     b) Build prompt with schema                                             │
│     c) Call OpenAI API                                                      │
│        └──► openai.chat.completions.create(...)                             │
│     d) clean_sql_query(response)                                            │
│        └──► "SELECT * FROM products WHERE price > 100"                      │
│                                     │                                       │
│                                     ▼                                       │
│  5. RESPONSE                                                                │
│     Returns to ui.py → Display SQL in code block                            │
│                                     │                                       │
│                                     ▼                                       │
│  6. EXECUTION (Optional)                                                    │
│     User clicks "Execute SQL"                                               │
│     POST /execute_sql → query_generator.execute_query()                     │
│     → MySQL returns data → Display in DataFrame                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🛡️ Security Considerations

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SECURITY BEST PRACTICES                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✓ Environment Variables                                                    │
│    - API keys stored in .env, not in code                                   │
│    - Add .env to .gitignore!                                                │
│                                                                             │
│  ✓ SQLAlchemy text()                                                        │
│    - Parameterized queries prevent SQL injection                            │
│    - Never concatenate user input directly into SQL                         │
│                                                                             │
│  ⚠️ Potential Risks (for production):                                       │
│    - Add authentication to API endpoints                                    │
│    - Rate limit OpenAI calls                                                │
│    - Validate/sanitize generated SQL before execution                       │
│    - Use read-only database user for query execution                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Technology Stack Summary

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         TECH STACK                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   FRONTEND                    BACKEND                    DATA               │
│   ┌─────────────┐            ┌─────────────┐            ┌─────────────┐     │
│   │  Streamlit  │            │   FastAPI   │            │   MySQL     │     │
│   │  (Python)   │─── HTTP ──►│   (Python)  │─── SQL ───►│  Database   │     │
│   │             │            │             │            │             │     │
│   │  • Rapid UI │            │  • REST API │            │  • Tables   │     │
│   │  • Widgets  │            │  • Pydantic │            │  • Queries  │     │
│   │  • Session  │            │  • Async    │            │  • Data     │     │
│   └─────────────┘            └──────┬──────┘            └─────────────┘     │
│                                     │                                       │
│                                     │ HTTPS                                 │
│                                     ▼                                       │
│                              ┌─────────────┐                                │
│                              │   OpenAI    │                                │
│                              │   GPT-4     │                                │
│                              │             │                                │
│                              │  • NL→SQL   │                                │
│                              │  • Context  │                                │
│                              └─────────────┘                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎓 Learning Path

1. **Beginner**: Understand the file structure and how requests flow
2. **Intermediate**: Modify prompts in `query_generator.py` for better SQL generation
3. **Advanced**: Add new endpoints, improve error handling, add authentication
4. **Expert**: Implement caching, query optimization suggestions, multi-database support

---

## 🔗 Resources for Further Learning

- **FastAPI**: [fastapi.tiangolo.com](https://fastapi.tiangolo.com)
- **Streamlit**: [docs.streamlit.io](https://docs.streamlit.io)
- **SQLAlchemy**: [docs.sqlalchemy.org](https://docs.sqlalchemy.org)
- **OpenAI API**: [platform.openai.com/docs](https://platform.openai.com/docs)
- **MySQL**: [dev.mysql.com/doc](https://dev.mysql.com/doc)

---

*Happy Learning! 🚀*
